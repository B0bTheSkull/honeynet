log_file: "logs/honeynet.json"

honeypots:
  ssh:
    enabled: true
    port: 2222
    banner: "SSH-2.0-OpenSSH_8.9p1 Ubuntu-3ubuntu0.6"

  http:
    enabled: true
    port: 8080

  ftp:
    enabled: true
    port: 2121
    banner: "220 ProFTPD 1.3.5e Server (Debian) ready."

alerts:
  multi_service_window: 60   # seconds — flag IPs that hit N+ services in this window
